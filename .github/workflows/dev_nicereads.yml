# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy ASP.Net Core app to Azure Web App - nicereads

on:
  push:
    branches:
      - dev

env:
  DOTNET_ROOT: '.'

jobs:

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Run Snyk to check for vulnerabilities
        uses: snyk/actions/dotnet@master
        continue-on-error: true # To make sure that SARIF upload gets called
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --sarif-file-output=snyk.sarif --file=.sln
      - name: Upload result to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@v1
        with:
          sarif_file: snyk.sarif

 # build-and-deploy:
 #   runs-on: windows-latest

 #   steps:
 #   - name: Check out
 #     uses: actions/checkout@master

 #   - name: Set up .NET Core
 #     uses: actions/setup-dotnet@v1
 #     with:
 #       dotnet-version: '5.0'
        
 #   - name: Install dependencies
 #     run: dotnet restore TOS.sln
      
 #   - name: Build
 #     run: dotnet build TOS.sln --configuration Release --no-restore
      
 #   - name: Test
 #     run: dotnet test TOS.sln --no-restore --verbosity normal
      
 #   - name: Publish
 #     run: dotnet publish TOS.sln -c Release -o ${{env.DOTNET_ROOT}}/myapp

    #- name: Deploy to Azure Web App
    #  uses: azure/webapps-deploy@v2
    #  with:
    #    app-name: 'nicereads'
    #    slot-name: 'production'
    #    publish-profile: ${{ secrets.AzureAppService_PublishProfile_2f19571d2c984f0abc50f8893e825a50 }}
    #    package: ${{env.DOTNET_ROOT}}/myapp 
